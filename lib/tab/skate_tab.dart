import 'package:flutter/material.dart';

class SkateTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("SKATE TAB"),
      ),
    );
  }
}
