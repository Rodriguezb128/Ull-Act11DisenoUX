import 'package:flutter/material.dart';

class BasketTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BASKET TAB"),
      ),
    );
  }
}
